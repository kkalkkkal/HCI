<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hci_project">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.HCI_Project">
        <activity android:name=".EnhancedCompareSchoolActivity"></activity>
        <activity android:name=".MainCopyActivity" />
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BarChartActivity" />
        <activity android:name=".LineChartActivity" />
        <activity android:name=".PieChartActivity" />
        <activity android:name=".CompareSchoolActivity" />
        <activity android:name=".SchoolInfoActivity" />
        <activity android:name=".SearchFilterActivity" />
        <activity
            android:name=".SearchSchoolActivity"
            android:windowSoftInputMode="stateVisible" />

        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="o82z0vth6u" />
    </application>

</manifest>